{
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/core",
      "packages/docs",
      "packages/example",
      "packages/laravel",
      "packages/ssr",
      "packages/vite",
      "packages/vue3"
    ]
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "pretty-quick": "^3.1.3",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "prepare": "husky install && yarn workspaces foreach run build",
    "dev": "yarn workspaces foreach --interlaced --parallel run dev",
    "release": "yarn workspaces foreach run release"
  },
  "packageManager": "yarn@3.2.4",
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
